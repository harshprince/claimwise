spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/harshprince/claimwise
          username: ${GIT_USER:}
          password: ${GIT_TOKEN:}

  security:
    user:
      name: configuser
      password: devpassword123

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

configserver:
  username: claimwise
  password: 123456

server:
  ssl:
    enabled: false
